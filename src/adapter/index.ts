import { api, askQuery, client, fetchGoodIdentificationTypes, getConfig, getGitBookPage, getProductIdentificationPref, getUserFacilities, initialise, logout, resetConfig, searchQuery, setProductIdentificationPref, updateInstanceUrl, updateToken } from '@hotwax/oms-api'

export {
  api,
  askQuery,
  client,
  fetchGoodIdentificationTypes,
  getConfig,
  getGitBookPage,
  getProductIdentificationPref,
  getUserFacilities,
  initialise,
  logout,
  resetConfig,
  searchQuery,
  setProductIdentificationPref,
  updateInstanceUrl,
  updateToken
}